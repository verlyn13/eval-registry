{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/verlyn13/eval-registry/main/schemas/authorization-state.v2.schema.json",
  "title": "Eval Registry authorization state v2",
  "description": "Closed authorization-state shape adding a mandatory pull-request review mode.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "stage",
    "repository_creation_approved",
    "pull_request_review_mode",
    "signer_workflow_approved",
    "external_authorities_approved",
    "record_publication_approved",
    "effective_date"
  ],
  "properties": {
    "schema_version": {"const": "eval-registry.authorization-state.v2"},
    "stage": {"type": "string", "minLength": 1},
    "repository_creation_approved": {"type": "boolean"},
    "pull_request_review_mode": {"const": "solo_maintainer_attestation_v1"},
    "signer_workflow_approved": {"type": "boolean"},
    "external_authorities_approved": {"type": "boolean"},
    "record_publication_approved": {"type": "boolean"},
    "effective_date": {"type": "string", "format": "date"}
  }
}
